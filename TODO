 must
 - move Executors to module
 - move Executor from ControllableImpl
 - pool like service
 - services must be ndependent on startup
 - add state checking in Cli
 - common service interface
 - fix app stopping
 - make the same start params for apps
 - make per-config-parser
 - move queue size to config
 - @BlackHoleQueue -> normal name
 may be
 - config: from class to properties
 - config per service
 - add service init-on-startup (@PostConstruct)
 - add instant queue size to JMX

